defaults:
  - plots:
    - training_curves
    - metric_trajectories
    # - method_comparison
    - tradeoff

file_type: png
max_ncols: 3

save_args:
  dpi: 150
  # bbox_inches: tight
  # pad_inches: 0