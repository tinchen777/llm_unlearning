# https://hydra.cc/docs/configure_hydra/intro/

# enable rich logging (requires `pip install rich`)
defaults:
  - override hydra_logging: colorlog   # Hydra's own framework logs
  - override job_logging: colorlog     # your application logs (the ones you usually care about)

# output directory, generated dynamically on each run
run:
  dir: ${paths.output_dir}

# sweep:
#   dir: ${paths.log_dir}/${task_name}/multiruns/${now:%Y-%m-%d}_${now:%H-%M-%S}
#   subdir: ${hydra.job.num}

job_logging:
  formatters:
    colorlog:
      format: "[%(asctime)s][%(cyan)s%(name)s%(reset)s][%(log_color)s%(levelname)s%(reset)s] - %(message)s"
      datefmt: "%Y-%m-%d %H:%M:%S"

  loggers:
    datasets:
      level: ERROR
      propagate: false

  handlers:
    file:
      filename: ${hydra.runtime.output_dir}/${hydra.job.name}_${now:%Y-%m-%d}_${now:%H-%M-%S}.log