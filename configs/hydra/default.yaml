# https://hydra.cc/docs/configure_hydra/intro/

# enable color logging
defaults:
  - override hydra_logging: colorlog
  - override job_logging: rich   # rich.logging.RichHandler (requires `pip install rich`).
  # To revert to the old style: change to `colorlog`, or per-run `hydra/job_logging=colorlog`.

# output directory, generated dynamically on each run
run:
  # dir: ${paths.save_dir}/${now:%Y-%m-%d}_${now:%H-%M-%S}
  dir: ${paths.output_dir}
# sweep:
#   dir: ${paths.log_dir}/${task_name}/multiruns/${now:%Y-%m-%d}_${now:%H-%M-%S}
#   subdir: ${hydra.job.num}

job_logging:
  handlers:
    file:
      # Incorporates fix from https://github.com/facebookresearch/hydra/pull/2242
      filename: ${hydra.runtime.output_dir}/${trainer.handler}.log